#include"iostream"
#include"string.h"
using namespace std;
void subsets(int *s,int n)
{
	for(int i=0;i<n;i++)
	{
		for(int j=1;j<i;j++)
		{
			
		}
	}
}
main()
{
	int s[]={1,2,3,4,5,6,7};
	int l=sizeof(s)/sizeof(int);
	subsets(s,l);
}
